<?php
//require_once('include/dbconnect');
//require_once('');

function getAnswer( $response ){
  $bits = explode( '=', $response, 2 );
  return array( trim($bits[0]), trim($bits[1]) );
//  return array( $bits[0] => $bits[1] );
}
?>
